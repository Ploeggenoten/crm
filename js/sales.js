/* MODULE: Sales — wordt in fase 1 gebouwd. */
CRM.registerModule('sales', {
  title:'Sales', icon:'◈', onderschrift:'Klantpijplijn en kansen',
  render(mount){
    mount.innerHTML = CRM.ui.leeg('Sales wordt gebouwd', 'Deze module staat klaar in het fundament en wordt nu ingevuld.');
  }
});
