/* MODULE: Recruitment — wordt in fase 1 gebouwd. */
CRM.registerModule('recruitment', {
  title:'Recruitment', icon:'◉', onderschrift:'Leads, intake en pijplijn',
  render(mount){
    mount.innerHTML = CRM.ui.leeg('Recruitment wordt gebouwd', 'Deze module staat klaar in het fundament en wordt nu ingevuld.');
  }
});
