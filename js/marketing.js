/* MODULE: Marketing — wordt in fase 1 gebouwd. */
CRM.registerModule('marketing', {
  title:'Marketing', icon:'◐', onderschrift:'Content en advertenties',
  render(mount){
    mount.innerHTML = CRM.ui.leeg('Marketing wordt gebouwd', 'Deze module staat klaar in het fundament en wordt nu ingevuld.');
  }
});
