/* MODULE: Kandidaten — wordt in fase 1 gebouwd. */
CRM.registerModule('kandidaten', {
  title:'Kandidaten', icon:'☰', onderschrift:'Kandidatenkaarten',
  render(mount){
    mount.innerHTML = CRM.ui.leeg('Kandidaten wordt gebouwd', 'Deze module staat klaar in het fundament en wordt nu ingevuld.');
  }
});
