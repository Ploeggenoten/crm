/* MODULE: Performance — wordt in fase 1 gebouwd. */
CRM.registerModule('performance', {
  title:'Performance', icon:'▲', onderschrift:'Prestaties en cijfers',
  render(mount){
    mount.innerHTML = CRM.ui.leeg('Performance wordt gebouwd', 'Deze module staat klaar in het fundament en wordt nu ingevuld.');
  }
});
