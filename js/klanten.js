/* MODULE: Klanten — wordt in fase 1 gebouwd. */
CRM.registerModule('klanten', {
  title:'Klanten', icon:'▣', onderschrift:'Klantkaarten en accountbeheer',
  render(mount){
    mount.innerHTML = CRM.ui.leeg('Klanten wordt gebouwd', 'Deze module staat klaar in het fundament en wordt nu ingevuld.');
  }
});
